[build]
  command = "npm run build"
  publish = ".next"
  framework = "next"

[functions]
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[context.deploy-preview.environment]
  FORCE_PROD_BLOBS = "true"

[context.branch-deploy.environment]
  FORCE_PROD_BLOBS = "true"
